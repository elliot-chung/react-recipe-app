import styled from "styled-components";
import StyledButton from "./Button.style";

const StyledEditModeButton = styled(StyledButton)`
  position: absolute;
  padding: 0.75rem;
  top: 1rem;
  right: 0;
`;

export default StyledEditModeButton;
